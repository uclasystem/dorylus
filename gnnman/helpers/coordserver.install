#!/bin/bash

##
## Install requried dependencies on a coordination server. TO be invoked on the remote machine.
##


# Basic packages.
cd ${HOME}/gnn-lambda/gnnman/helpers/
source basic.install


# ZeroMQ stuff.
cd ${HOME}/gnn-lambda/gnnman/helpers/
source zmq.install


# AWS API stuff.
cd ${HOME}/gnn-lambda/gnnman/helpers/
source aws.install
